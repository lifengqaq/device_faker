# 机型伪装配置文件
# 为不同的应用配置不同的伪装机型

# ============================================================
# 全局配置
# ============================================================

# 全局设置
#
# debug = true 可开启详细日志（默认关闭，隐蔽性更好）
# default_force_denylist_unmount = true 可全局启用 FORCE_DENYLIST_UNMOUNT

# 调试模式（默认关闭以提高隐蔽性）
# 设置为 true 后详细日志写入 /data/adb/device_faker/logs/device_faker.log
# 正常使用时建议设置为 false 或删除此行
# debug = false

# ============================================================
# 机型模板定义
# ============================================================
# 定义常用机型模板，可以直接在模板中指定包名列表

# ── SoC 伪装字段（游戏开高帧率的关键）────────────────────────────
# hardware  → ro.hardware          （SoC 厂商硬件名，如 qcom / mt6989）
# board     → ro.product.board     （主板平台代号，同时回退写入 ro.board.platform）
# soc_platform    → ro.board.platform 平台族（kalama / pineapple / sun / mt6989 …）
# soc_model       → ro.soc.model 型号族 + Build.SOC_MODEL（SM8650 / MT6989 …）
# soc_manufacturer→ ro.soc.manufacturer（Qualcomm / MediaTek / Samsung …）
#
# 三个 soc_* 字段都会「整族写入」多个别名 key（ro.vendor.qti.* / ro.mediatek.* /
# ro.hardware.chipname / ro.chipname 等），避免游戏从任一别名读回真机 SoC 后锁帧。
# 若真机与目标 SoC 跨厂商（如真机联发科、伪装成骁龙），必须
# companion_resetprop = true，否则新出现的别名 key 无法在纯 COW 模式下创建。

[templates.redmagic_11_pro]
packages = [
    "com.mobilelegends.mi",
    "com.supercell.brawlstars",
    "com.blizzard.diablo.immortal",
    "com.netease.newspike",
    "com.activision.callofduty.warzone",
]
manufacturer = "Nubia"
brand = "REDMAGIC"
model = "NX809J"
device = "REDMAGIC 11 PRO"
product = "NX809J"
hardware = "qcom"
board = "sun"
soc_platform = "sun"
soc_model = "SM8850"
soc_manufacturer = "Qualcomm"
fingerprint = "REDMAGIC/NX809J-UN/NX809J:16/BP2A.250605.031.A3/20251017.000000:user/release-keys"
build_id = "BP2A.250605.031.A3"

[templates]
[templates."一加15"]
manufacturer = "OnePlus"
brand = "OnePlus"
model = "PLK110"
device = "PLK110"
product = "PLK110"
hardware = "qcom"
board = "canoe"
soc_platform = "canoe"
soc_model = "SM8850"
soc_manufacturer = "Qualcomm"
fingerprint = ""
packages = [ "com.tencent.tmgp.pubgmhd", "com.tencent.tmgp.supercell.clashofclans", "com.tencent.tmgp.speedmobile" ]

[templates.IQOO15]
manufacturer = "VIVO"
brand = "VIVO"
model = "V2505A"
device = "V2505A"
product = "V2505A"
hardware = "qcom"
board = "canoe"
soc_platform = "canoe"
soc_model = "SM8850"
soc_manufacturer = "Qualcomm"
fingerprint = ""
packages = [ "com.tencent.tmgp.sgame", "com.tencent.tmgp.codev" ]

# ============================================================
# 应用配置
# ============================================================

# 方式一：使用模板的包名列表
# 直接在上面的模板中添加包名到 packages 列表即可
# 不需要在这里单独配置

# 方式二：直接配置
# 不使用模板，直接指定设备信息

[[apps]]
package = "com.coolapk.market"
manufacturer = "Nothing"
brand = "Nothing"
marketname = "Nothing Phone (3)"
model = "A024"

[[apps]]
package = "com.tencent.tmgp.pubgmhd"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.sgame"
model = "V2505A"
manufacturer = "VIVO"
brand = "vivo"

[[apps]]
package = "com.rekoo.pubgm"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.cf"
model = "NP05J"
brand = "NUBIA"
manufacturer = "NUBIA"

[[apps]]
package = "com.tencent.lolm"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.vng.pubgmobile"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.dfm"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.mf.uam"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.cod"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.jkchess"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.speedmobile"
model = "PLK110"
manufacturer = "OnePlus"
brand = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.supercell.clashofclans"
model = "PLK110"
brand = "OnePlus"
manufacturer = "OnePlus"

[[apps]]
package = "com.tencent.tmgp.codev"
model = "V2505A"
brand = "VIVO"
manufacturer = "vivo"

[[apps]]
package = "flar2.devcheck"
model = "V2505A"
brand = "VIVO"
manufacturer = "vivo"

# ============================================================
# 配置说明
# ============================================================
#
# 【架构】按需调度（无需选择模式）
#
# 所有应用自动走统一执行流：
#   ① Build 字段覆写 (JNI)      — 始终执行（如有设备字段）
#   ② 属性伪装                   — companion_resetprop = true 时全部走
#                                  companion resetprop；否则 COW 优先，
#                                  COW 未找到的属性与 __DELETE__ 走 companion
#   ③ DPI 伪装                   — 按需（仅设置 dpi 时）
#   ④ DlClose                    — 始终执行（零模块驻留）
#
# 用户不再需要理解 lite/full/companion 的区别。
#
# 【配置方式】
# 1. 模板包名列表 - 在模板的 packages 中直接添加包名
# 2. 直接配置 - 使用 [[apps]] 直接指定设备信息
#
# 【优先级】
# 字段优先级：[[apps]] 直接配置 > 模板 packages 列表
#
# 【COW 引擎】
# - 通过 mmap COW 重映射 prop_area 文件，直接覆写属性内存
# - 覆盖 __system_property_get 和 __system_property_read_callback（同一份 mmap）
# - 无 GOT/PLT 修改，无 JNI native method 替换
# - 模块写完即 DlClose，/proc/self/maps 干净
#
# 【__DELETE__ 标记】
# 设置属性值为 "__DELETE__" 可通过 companion resetprop 删除该属性。
# 设置属性值为 "__EMPTY__" 将属性值设为空字符串。
#
# 【DPI 伪装】
# 在模板或 [[apps]] 中设置 dpi = 420。
# 保存原始值，并在应用退后台/退出后自动恢复（切后台约 0.5s 后恢复）。